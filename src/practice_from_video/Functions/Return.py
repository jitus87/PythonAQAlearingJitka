# returns a value as a result of a function, which can be used in the code

def square(number):
    return number*number


result = square(3)
print(result)               # or we can use only:   print(square(3))